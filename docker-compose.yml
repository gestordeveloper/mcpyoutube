version: '3.8'

services:
  mcpyoutube:
    image: agenciaebenezer/mcpyoutubeportainer:v1.0.1
    ports:
      - 3839:3839
    environment:
      - PORT=3839
    networks:
      - web
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.role == manager

networks:
  web:
    external: true
    name: easypanel
